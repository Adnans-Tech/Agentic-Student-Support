# Student Support System - Dependencies

# LangChain Core
langchain>=0.1.0
langchain-core>=0.1.0
langchain-community>=0.0.38
langchain-groq>=0.0.1
langgraph>=0.0.20

# Vector Store & Embeddings
faiss-cpu>=1.7.4
sentence-transformers>=2.2.0
langchain-text-splitters>=0.0.1
chromadb>=0.4.0

# Email Service
sendgrid>=6.10.0

# LLM API (still needed for direct calls)
groq>=0.4.0

# Flask Web Interface
flask>=2.3.0
flask-cors>=4.0.0

# Authentication
PyJWT>=2.8.0
Werkzeug>=3.0.0

# Production Chat Memory (choose one)
# Set CHAT_MEMORY_BACKEND env var: "chromadb" (default), "upstash", or "sqlite"
upstash-redis>=1.0.0  # Serverless Redis (recommended for Vercel/AWS Lambda)
redis>=5.0.0  # Self-hosted Redis alternative

# Excel Import (for bulk data import)
openpyxl>=3.1.0
pandas>=2.0.0

# Utilities
pytz>=2023.3
python-dotenv>=1.0.0
